# assets

평의회 장면 배경 아트를 여기에 둔다.

- `council.png` — 제목 + 세 여신(여명·황혼·잿불) 카드 + 설명이 담긴 전체 장면 이미지.
  - 맨 아래 "운명이 말하다" 박스는 앱이 실시간 결과로 덧씌우므로, **그 박스를 비워서**
    내보내면 가장 깔끔하다(없으면 불투명 오버레이로 덮는다).

`python -m three_sisters.scene_html` 가 이 이미지를 배경으로 `council.html` 을 만든다.
